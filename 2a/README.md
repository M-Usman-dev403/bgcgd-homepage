# BGCGD homepage redesign — 2a (dark)

Static HTML/CSS. No build step, no dependencies.

- `index.html` — the page
- `img/` — photography, press logos, logo, hero video

Fonts load from Google Fonts (Inter, Barlow Condensed).
